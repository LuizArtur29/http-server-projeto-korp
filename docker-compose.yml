services:
  http-server-projeto-korp:
    image: http-server-projeto-korp:local
    networks:
      - korp-network
    expose:
      - "8080"
    read_only: true
    security_opt:
      - no-new-privileges:true
    mem_limit: 128m
    cpus: 0.50
    labels:
      org.opencontainers.image.title: "http-server-projeto-korp"
      org.opencontainers.image.description: "Projeto Korp DevOps challenge"
      com.korp.project: "http-server-projeto-korp"
      com.korp.service: "application"
      com.korp.managed-by: "docker-compose"

  nginx:
    image: nginx:1.27.5-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d:ro
    networks:
      - korp-network
    healthcheck:
      test:
        - CMD-SHELL
        - "wget -q -O /dev/null http://127.0.0.1/healthz || exit 1"
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    mem_limit: 128m
    cpus: 0.50
    labels:
      org.opencontainers.image.title: "http-server-projeto-korp"
      org.opencontainers.image.description: "Projeto Korp DevOps challenge"
      com.korp.project: "http-server-projeto-korp"
      com.korp.service: "reverse-proxy"
      com.korp.managed-by: "docker-compose"

  prometheus:
    image: prom/prometheus:v3.14.0
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "127.0.0.1:9090:9090"
    networks:
      - korp-network
    depends_on:
      - http-server-projeto-korp
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - wget
        - "--spider"
        - "-q"
        - "http://localhost:9090/-/ready"
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 10s
    mem_limit: 256m
    cpus: 0.75
    labels:
      org.opencontainers.image.title: "http-server-projeto-korp"
      org.opencontainers.image.description: "Projeto Korp DevOps challenge"
      com.korp.project: "http-server-projeto-korp"
      com.korp.service: "monitoring"
      com.korp.managed-by: "docker-compose"

  grafana:
    image: grafana/grafana:13.2.1
    environment:
      GF_SECURITY_ADMIN_USER: ${GF_SECURITY_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD:?GF_SECURITY_ADMIN_PASSWORD is required}
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - korp-network
    depends_on:
      - prometheus
    restart: unless-stopped
    healthcheck:
      test:
        - CMD-SHELL
        - "wget -q --spider http://localhost:3000/api/health || exit 1"
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 15s
    mem_limit: 256m
    cpus: 0.75
    labels:
      org.opencontainers.image.title: "http-server-projeto-korp"
      org.opencontainers.image.description: "Projeto Korp DevOps challenge"
      com.korp.project: "http-server-projeto-korp"
      com.korp.service: "visualization"
      com.korp.managed-by: "docker-compose"

networks:
  korp-network:
    name: korp-network
    external: true